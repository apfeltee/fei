
  # void initScanner(const char *source);
  "initScanner": "fei_lexer_init"

  # static _Bool isAplha(char c);
  "isAplha": "fei_lexutil_isalpha"

  # static _Bool isDigit(char c);
  "isDigit": "fei_lexutil_isdigit"

  # static _Bool isAtEnd(void);
  "isAtEnd": "fei_lexer_isatend"

  # static char advance(void);
  "advance": "fei_lexer_advance"

  # static _Bool match(char expected);
  "match": "fei_lexer_match"

  # static Token makeToken(TokenType type);
  "makeToken": "fei_lexer_maketoken"

  # static Token errorToken(const char *message);
  "errorToken": "fei_lexer_makeerrortoken"

  # static char fei_vm_peek(void);
  "fei_vm_peek": "fei_vm_peek"

  # static char peekNext(void);
  "peekNext": "fei_lexer_peeknext"

  # static void skipWhiteSpace(void);
  "skipWhiteSpace": "fei_lexer_skipspace"

  # static TokenType checkKeyword(int start, int length, const char *rest, TokenType type);
  "checkKeyword": "fei_lexer_checkkeyword"

  # static TokenType identifierType(void);
  "identifierType": "fei_lexer_scantype"

  # static Token identifier(void);
  "identifier": "fei_lexer_scanident"

  # static Token number(void);
  "number": "fei_lexer_scannumber"

  # static Token string(void);
  "string": "fei_lexer_scanstring"

  # Token scanToken(void);
  "scanToken": "fei_lexer_scantoken"

